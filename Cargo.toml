[workspace]
resolver = "2"
members = [
    "sigalign-core",
    "sigalign-impl",
    "sigalign-utils",
    "sigalign",
    "sigalign-py",
    "tests",
    "examples/sigalign-demo-aligner",
    "examples/sigalign-demo-wasm",
]
exclude = [
    "examples/sigalign-demo-web",
]

[profile.dev]
opt-level = 1

[profile.release]
lto = true
opt-level = 3

[profile.bench]
opt-level = 3
