[workspace]
resolver = "2"
members = [
    "sigalign",
    "sigalign-py",
    "tests",
    "examples/sigalign-demo-aligner",
    "examples/sigalign-demo-wasm",
]
exclude = [
    "examples/sigalign-demo-web",
    "masking_debug",
]

[profile.dev]
opt-level = 1

[profile.release]
lto = true
opt-level = 3
